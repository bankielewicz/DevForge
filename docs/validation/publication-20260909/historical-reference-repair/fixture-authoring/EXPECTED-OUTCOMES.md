# Frozen R2 fixture outcomes, before test source edits

Authority: parent ALLOCATION.json 3fb2e4bb2fe18db22b018e8de9505a1b3780e92624f9dcadc52aea0ff6090b0e; preserved contract fc7d5901899882c1fb6762e5e4683e978212d35ccfc484df1cd5d5fa61807b90; prior findings a92b7088ef29a8c1e46132eb5733c75199a45e11deb380f692933fa2411517d2. HEAD-RECONCILIATION SHA-256 b8f4292466ebcc86f7531bc3cd56310c3b2b78320111e2da4c8b08e2da42fc05. Expectations below were frozen before edits and do not come from candidate execution or reading root's new implementation.

1. Complete explicit artifact references in upstream, execution_ref, decision_ref and evidence require the sections key. Positive counterparts retain their sections arrays. Body upstream/execution_ref/decision_ref/evidence/output atoms also require it. For non-upstream body pairs, use explicit empty sections on the valid atom and matching envelope when applicable, so refusal of the missing-key atom cannot be explained only by an unrelated envelope-section mismatch. Expected missing-key result: FAIL and no receipt.
2. Same-ID older selected supersedes remains valid with omitted sections in both envelope and body. SESSION shorthand remains valid, with a complete explicit body repetition using sections []. Expected: PASS under the selected-reference seam.
3. A real source table with leading outer pipe omitted, trailing outer pipe omitted, both omitted, or both present supplies its exact first-cell I-001 row under a real SRC-001 heading. Expected: PASS. SHA-256/SHA256 header tables with those same border forms and short data claims fail in plain text, fenced code, and HTML comments. Their failure must not depend on an unrelated 64-character hash or selected locator.
4. A header/separator followed by a fenced, indented, or comment block cannot supply a header for a later orphan pipe row. Expected source_rows claim: FAIL. An adjacent real table with its own header after the same block passes, as does a contiguous table.
5. Per the newly recorded prospective RRC-007 interpretation, malformed markers and syntactically valid atoms with incorrect selected-target digests fail in producer.skill, producer.skill_revision, ordinary missing-input strings, structured missing_inputs.reason, external-research.claim and external-research.applicable_version. A valid SESSION evidence atom repeated in these metadata strings must produce a distinct occurrence for every repetition, with the exact document digest, target tuple and UTF-8 byte range. Expected positive count: seven atoms across six named locations, including two in one value. The old interpretation-sensitive review is preserved.
6. Body-only reserved-token restrictions do not apply to ordinary metadata. producer.skill_revision equal to sha256:<complete selected hash> remains valid when no explicit atom is present. Structured reference tuple values are never rescanned as free text: an explicitly selected raw-file logical path containing literal @df-ref({BROKEN}) is a path value, not a nested atom. Expected: PASS with exact raw tuple binding.

All expected values are constructed from literal synthetic fixture text, selected fixture bytes and their ordinary hashes. Production calls remain only the existing check/finalize/verify seams inside authored tests. No test is run by this author. Later execution requires separately selected source and fixture hashes.
