NI-11 adds the owned app-server transport for frozen exact-question answers and explicit follow-up turns. Continuation units participate in complete-allocation admission, are claimed durably before a model-generating send, and retain the original attempt/campaign deadlines. Collection, teardown, transcript replay and receipt qualification are integrated with the protected journal. Managed follow-up turns require the exact fully written prompt sequence and observed waiting-phase resumption; additional arbitrary callbacks do not satisfy completion.

Independent review found the former single-prompt completion rule incompatible with managed continuations and required stronger accounting/replay/cleanup coverage. Those source-level findings are closed in review-03. Review-02 stopped on a root message formatting error in the supplied manifest digest; its custody-blocked record and charged call remain preserved. All frozen snapshot bytes were retained.

The combined framework-validation and NI-11 tree passed cargo fmt/clippy/build/test, 572 Python tests, the fixture lifecycle demo, and structural validation of both the frozen committed companion and observed shared companion source. Exact commands and hashes are in combined-checks/results.json. These results do not establish native Codex behavior, effective isolation, authenticated callback origin, or actual receiving execution. No model invocation or authentication was performed in this implementation checkpoint.

The transport intentionally rejects an unanswered or unmatched question. Case-specific natural-language operator matching, serial nested evaluation jobs, expanded parent/campaign deadlines and per-attempt non-model prerequisite probing still require a reviewed execution arrangement before the complete larger evaluation can run. The current 24-call and original clock limits are unchanged. Prepared allocation is not native admission.
