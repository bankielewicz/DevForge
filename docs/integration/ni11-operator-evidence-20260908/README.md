NI-11 now supports finite operator answer policies for actual blocking questions and completed assistant turns. The collector binds each choice to the real request, exact policy, attempt and continuation unit; it reserves before transmission, preserves original deadlines, and retains signed request/decision/replay custody. Missing, stale, malformed or oversized operator documents stop collection without a fabricated answer. Owned process cleanup remains required.

Independent review04 found a maximum-size request wrapping defect. Review05 closes it: the complete operator document is bounded before publication, and the exact 8 MiB boundary stays readable and replayable. Both the original finding and RED/GREEN evidence are retained.

Final Rust formatting, locked clippy/build/all-target tests, frozen/shared framework validation and demo passed. The final unchanged Python suite passed579 tests. Its preceding run failed one compiled scheduling test with a journal clock high-water rejection; the unchanged focused test and one full followup passed. The transient root cause is unverified; the failed run is not erased or relabeled. Final check evidence combines combined-checks-03/results.json for non-Python checks and combined-checks-03/python-full-followup-01.json for the Python result.

These are source review and deterministic/synthetic process results. No authenticated native client, native hook boundary, real operator identity, semantic acceptance or receiving execution was observed. The complete native evaluation still needs separately reviewed nested broker/resume, finite budget/deadline amendments, per-attempt command/exec preflight and compiled allocation admission. This component does not authorize those missing interfaces or a native campaign.

Canonical DevForgeAI skills remain at9970e92db7d824dea0c49e1a61a122480f2aa8b5 and their installed operational bytes remain identical; no refresh was needed. Unrelated concurrent files are preserved outside this commit.
